# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...   # from Stripe Dashboard → Webhooks

# Upstash Redis (create free DB at upstash.com)
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=...

# JWT — generate a strong random secret: openssl rand -base64 32
JWT_SECRET=...

# Resend (already in use)
RESEND_API_KEY=re_...

# Vercel AI Gateway — used by /api/generate-narrative to power the personalised
# Blueprint narrative (executive diagnosis, bottleneck deep-dive, commitment).
# Get from: https://vercel.com/dashboard → AI Gateway → API Keys
AI_GATEWAY_API_KEY=
